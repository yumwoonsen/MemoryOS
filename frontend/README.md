# Frontend placeholder

Phase 1 intentionally proves the Memory Engine before UI work begins. A later Next.js client can
consume `POST /v1/memories/discover` without changing the versioned input or output contracts.

